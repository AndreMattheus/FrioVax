# Copie para .env apenas se quiser mudar os padrões locais do docker-compose.yml.
# Não use credenciais reais: estes valores servem só para o ambiente de desenvolvimento.
DB_NAME=friovax
DB_USER=friovax
DB_PASSWORD=friovax
DB_PORT=5432
